/**
 * Creating a playbook of servers and managing it with Ansible, tips & commands
 */

// Create a container and customize it as you want
// ..

// Create an image from the custom container with Docker commit
// docker container commit container_id user_name/myapp:1.0

// Create a Heroku account at
https://www.heroku.com/

// Install the Heroku cli tools
https://devcenter.heroku.com/articles/heroku-cli

// Deploy a Docker image into a container hosted by Heroku
heroku container:login
git clone https://github.com/heroku/alpinehelloworld.git // Actually, the only thing you need here is a folder that contains a Dockerfile within
heroku create app_name
cd folder_with_docker_file/
heroku container:push -a app_name web                   // The command won't work if you're not inside the folder that has a Dockerfile
heroku open -a app_name
